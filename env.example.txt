# Twilio Credentials
# Get these from: https://console.twilio.com
# Copy this file to .env (or .env.local) for local development – never commit real values.
# For Vercel: set these in Project → Settings → Environment Variables.

# Account SID (found in Twilio Console Dashboard)
TWILIO_ACCOUNT_SID=your_account_sid_here

# Auth Token (found in Twilio Console → Account → Auth Token)
TWILIO_AUTH_TOKEN=your_auth_token_here

# Verify Service SID (found in Twilio Console → Verify → Services)
TWILIO_VERIFY_SERVICE_SID=your_verify_service_sid_here

# Send order SMS (Twilio Messages API)
# Your Twilio number that sends the SMS (the "from" number)
TWILIO_PHONE_NUMBER=+12184033816
# Number that receives order details by SMS (e.g. 0543456305 → +972543456305)
ORDER_SMS_TO_NUMBER=+972543456305

# Google Sheets (for coupon system)
GOOGLE_SHEET_ID=your_google_sheet_id_here
GOOGLE_SHEET_NAME=Sheet1

